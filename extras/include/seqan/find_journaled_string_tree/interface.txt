Concept for JstTraversalConcept

MF: GetState<T>::Type	

FN: void 			  execute(ext, del, trav, tag)
	GetState<T>::Type getState(ext)
	void 			  setState(ext, state)
	
JstFinderExtensionConcept
	: extends JstTraversalConcept

Interface for ExtensionFunctor

template <typename TObject, typename TSpec>
class ExtensionFunctor
{

	operator()(Result, Iterator) : void
};

MF:

FinderExtension
[GetState] : Default Nothing  -> refinement
[ContextBeginPosition] : Default ContextBeginLeft
[RequireFullContext] : Default True

FN:

init() : void
[setState(ExtensionFunctor, State) : void] -> refinement
[getState(ExtensionFunctor) : ExtensionState] -> refinement
[execute(Result, ExtensionFunctor, Iterator) : void] -> refinement